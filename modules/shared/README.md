# shared module

Shared helpers used by all other reusable modules.

## What is inside

- `http.ts`: JSON fetch wrapper with base URL and credentials support
- `env.ts`: common manifest and environment requirement types

## Use it for

- client API wrappers
- module manifests
- lightweight reusable integrations
